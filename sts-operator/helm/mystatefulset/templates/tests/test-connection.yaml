apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mystatefulset.fullname" . }}-test-connection"
  labels:
    {{- include "mystatefulset.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mystatefulset.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
